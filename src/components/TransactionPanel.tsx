import { Transaction } from '@ethersproject/transactions';
import { Flex, Text } from '@theme-ui/components';
import React from 'react';

const TransactionPanel = ({ tx }: { tx: Transaction }) => {
  return <p>Hello</p>;
};

export default TransactionPanel;
